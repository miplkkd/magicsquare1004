# MagicSquare_1004 — RED 세션 보고서 (D-LOC-01)

**버전:** 0.1  
**작성일:** 2026-06-04  
**Phase:** RED  
**Layer:** entity  
**Track:** Logic  
**근거:** [docs/RED-Phase-Todo.md](../docs/RED-Phase-Todo.md), `.cursorrules`, Dual-Track TDD Skill

---

## 1. 요약

| 항목 | 내용 |
|------|------|
| **Test ID** | D-LOC-01 |
| **목표** | G1 격자에서 빈칸(0) 좌표를 **1-index, row-major**로 반환 (`find_blank_coords`) |
| **RED 결과** | `pytest.fail` 의도적 실패 — **PASS (RED 성공)** |
| **다음 Phase** | GREEN — `src/entity/`에 `find_blank_coords` 최소 구현 |

---

## 2. 설계표 (D-LOC-01)

| Test ID | Given | Then (기대값) | Expected RED Failure |
|:--------|:------|:--------------|:---------------------|
| **D-LOC-01** | G1 격자 (0이 2개, row-major) | `find_blank_coords(grid)` → `[(2, 2), (3, 3)]` (1-index) | `pytest.fail()` RED |

### G1 격자 (row-major)

```
[ 1,  2,  3,  4]
[ 5,  0,  7,  8]
[ 9, 10,  0, 12]
[13, 14, 15, 16]
```

- 빈칸 위치: **(2, 2)**, **(3, 3)** — 1-index (행, 열)
- 픽스처 검증: `entity.constants`의 `GRID_SIZE=4`, `BLANK_COUNT=2`

---

## 3. 산출물

### 3.1 테스트 (RED 스켈레톤)

| 파일 | 역할 |
|------|------|
| `tests/conftest.py` | `grid_g1` 픽스처, G1 격자 정의 |
| `tests/entity/test_d_loc_01.py` | D-LOC-01 RED 테스트 (AAA + `pytest.fail`) |

### 3.2 선행 SSOT (픽스처 import용)

| 파일 | 역할 |
|------|------|
| `src/entity/constants.py` | `GRID_SIZE`, `LINE_SUM_TARGET`, `CELL_MAX`, `BLANK_COUNT` |

> RED 스켈레톤 본문은 `tests/`만 추가. `constants.py`는 픽스처의 `from entity.constants import …` 규칙을 위한 MagicConstant SSOT.

### 3.3 문서·체크리스트 (동일 세션 맥락)

| 파일 | 역할 |
|------|------|
| `docs/RED-Phase-Todo.md` | Dual-Track RED 설계표·Todo SSOT |
| `README.md` | RED Phase 체크리스트 (진행용) |

---

## 4. Test Loop 실행

```powershell
python -m pytest tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major -v
```

| 항목 | 결과 |
|------|------|
| 수집 | 1 item |
| 결과 | **1 failed** (의도적) |
| FAIL 메시지 | `Failed: RED: D-LOC-01 — 구현 없음, 의도적 실패` |

---

## 5. 기술 이슈 및 조치

| 이슈 | 원인 | 조치 |
|------|------|------|
| `ModuleNotFoundError: entity.constants` | `tests/entity/` 패키지가 `src/entity`와 이름 충돌 | `tests/conftest.py`에서 `src`를 `sys.path` 선두에 추가 |
| `No module named 'entity.test_d_loc_01'` | `tests/entity/__init__.py` 존재 시 pytest가 테스트를 `entity` 하위 모듈로 수집 | **`tests/entity/__init__.py` 제거** |

---

## 6. RED 규칙 준수

| 규칙 | 준수 |
|------|------|
| AAA 주석 (Given / When / Then) | ✅ |
| Then은 `pytest.fail` 한 줄만 | ✅ |
| assert 본문·skip·xfail·통과 더미 없음 | ✅ |
| Logic Track Domain Mock 없음 | ✅ |
| `find_blank_coords` prod 구현 없음 | ✅ (GREEN 대기) |

---

## 7. Dual-Track RED 진행 현황

| Track | ID | RED 스켈레톤 | 비고 |
|-------|-----|-------------|------|
| Logic | **D-LOC-01** | ✅ | 본 보고서 |
| Logic | D-001 ~ D-010 | ⬜ | [docs/RED-Phase-Todo.md](../docs/RED-Phase-Todo.md) |
| Boundary | U-IN / U-OUT / U-FLOW | ⬜ | UI Track 대기 |

---

## 8. GREEN 힌트 (다음 세션)

1. `src/entity/`에 `find_blank_coords(grid: list[list[int]]) -> list[tuple[int, int]]` 추가
2. G1 입력 시 `[(2, 2), (3, 3)]` 반환 (1-index, row-major)
3. `test_d_loc_01.py`의 `pytest.fail` 제거 → assert로 Then 검증
4. `pytest tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major -v` → **PASS**

---

## 9. 관련 문서

- [01.MagicSquare_ProblemDefinition_Report.md](01.MagicSquare_ProblemDefinition_Report.md)
- [docs/PRD.md](../docs/PRD.md)
- [docs/RED-Phase-Todo.md](../docs/RED-Phase-Todo.md)
- [prompting/08.transcript-red-d-loc-01-export.md](../prompting/08.transcript-red-d-loc-01-export.md)
