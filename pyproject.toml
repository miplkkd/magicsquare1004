[project]
name = "magicsquare1004"
version = "0.1.0"
description = "MagicSquare_1004 — 4x4 partial magic square validation (ECB + Dual-Track TDD)"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra"
